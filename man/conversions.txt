Expressed types: Int, Bool, String, Char, Array

Int + - * / % Int = Int
- Int = Int
Int == != < > <= >= Int = Bool
Int + String = String
Int + - Char = Char

Bool == != & | Bool = Bool
Bool + String = String

String + String = String
String + Char = String

Char + Char = String

Bool + Char = String